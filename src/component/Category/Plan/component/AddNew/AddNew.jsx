import React from 'react';
import style from './AddNew.module.scss';

const AddNew = () => (
    <div className={style.container}>
        <button className={style.button}>+&nbsp; New Plan</button>
    </div>
);

export default AddNew;